import { ItemDescriptionPipe } from './item-description.pipe';

describe('ItemDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new ItemDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
